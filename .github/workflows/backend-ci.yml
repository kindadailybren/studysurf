name: Backend ( FastAPI ) CI

on:
  push:
    branches-ignore: [ main ]
    paths:
      - 'backend/**'
  pull_request:
    paths:
      - 'backend/**'

jobs:
  lint-code:
    uses: ./.github/workflows/backend-deploy.yml
    secrets: inherit
